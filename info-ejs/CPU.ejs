<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Form</title>
  <link rel="shortcut icon" href="data/icon.ico"/>
  <style>
  p {
   font-size:14px;
  }
  hr {
    color: black;
  }
  input {
   font-size:24px;
  }
  select {
    width: 500px; /* Ширина списка в пикселах */
    height: 75px;
  }
  body
  {
    background: #c7b39b url(data/BG.jpg); /* Цвет фона и путь к файлу */
  }
  .topnav { /*Верхняя панель*/
  overflow: hidden;
  background-color: #333;
  }

  .topnav a {
  float: left;
  color: #f2f2f2;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  font-size: 17px;
  }

  .topnav a:hover {
  background-color: #ddd;
  color: black;
  }

  .topnav a.active {
  background-color: #4CAF50;
  color: white;
  }
  .con {
    width: 500px;
    height: 100px;
    border: 2px solid black;
    margin: 4px;
  }
  </style>
</head>
<body>
  <header class="topnav">
    <a href="/">Главная страница</a>
    <a href="/results">Проверка совместимости</a>
    <a href="/catalog">Характеристика комплектующих</a>
    <a href="#">О проекте</a>
  </header>
  <main>
    <h1><%=dat[1].models[CPUid].name%></h1>
    <hr>
    <h2>Характеристики:</h2>
    <p>Сокет: <%=dat[1].models[CPUid].socket%></p>
    <p>Количество производительных ядер: <%=dat[1].models[CPUid].cores%></p>
    <p>Максимальное число потоков: <%=dat[1].models[CPUid].streams%></p>
    <p>Базовая частота процессора: <%=dat[1].models[CPUid].frequency%> ГГц</p>
    <p>Максималная частота в турбо режиме: <%=dat[1].models[CPUid].accel%> ГГц</p>
    <p>Кэш L1: <%=dat[1].models[CPUid].casheL1%></p>
    <p>Кэш L2: <%=dat[1].models[CPUid].casheL2%></p>
    <p>Кэш L3: <%=dat[1].models[CPUid].casheL3%></p>
    <p>Тип ОЗУ: <%=dat[1].models[CPUid].mem_type%></p>
    <p>Макс. частота ОЗУ: <%=dat[1].models[CPUid].max_mem_freq%> МГц</p>
    <p>Версия PCI-E: <%=dat[1].models[CPUid].pcie_vers%></p>
    <p>Число линий PCI-E: <%=dat[1].models[CPUid].pcie_lines%></p>
    <p>Интегрированное графическое ядро: <%=dat[1].models[CPUid].graphics%></p>
    <p>Тепловыделение(TDP): <%=dat[1].models[CPUid].tdp%> Вт</p>
    <p>Максимальная температура: <%=dat[1].models[CPUid].max_temp%></p>
  </main>
</body>
</html>
