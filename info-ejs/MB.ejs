<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Form</title>
  <link rel="shortcut icon" href="data/icon.ico"/>
  <style>
  p {
   font-size:14px;
  }
  hr {
    color: black;
  }
  input {
   font-size:24px;
  }
  select {
    width: 500px; /* Ширина списка в пикселах */
    height: 75px;
  }
  body
  {
    background: #c7b39b url(data/BG.jpg); /* Цвет фона и путь к файлу */
  }
  .topnav { /*Верхняя панель*/
  overflow: hidden;
  background-color: #333;
  }

  .topnav a {
  float: left;
  color: #f2f2f2;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  font-size: 17px;
  }

  .topnav a:hover {
  background-color: #ddd;
  color: black;
  }

  .topnav a.active {
  background-color: #4CAF50;
  color: white;
  }
  .con {
    width: 500px;
    height: 100px;
    border: 2px solid black;
    margin: 4px;
  }
  </style>
</head>
<body>
  <header class="topnav">
    <a href="/">Главная страница</a>
    <a href="/results">Проверка совместимости</a>
    <a href="/catalog">Характеристика комплектующих</a>
    <a href="#">О проекте</a>
  </header>
  <main>
    <h1><%=dat[0].models[MBid].name%></h1>
    <hr>
    <h2>Характеристики:</h2>
    <p>Чипсет: <%=dat[0].models[MBid].chipset%></p>
    <p>Сокет: <%=dat[0].models[MBid].socket%></p>
    <p>Форм-фактор: <%=dat[0].models[MBid].formfact%></p>
    <p>Слотов под ОЗУ: <%=dat[0].models[MBid].ram_slots%> шт.</p>
    <p>Тип ОЗУ: <%=dat[0].models[MBid].ram_type%></p>
    <p>Макс. частота ОЗУ: <%=dat[0].models[MBid].max_ram_freq%> МГц</p>
    <p>Количество портов SATA: <%=dat[0].models[MBid].sata_ports%></p>
    <p>Версия PCI-E: <%=dat[0].models[MBid].vers_pcie%></p>
    <p>Количество слотов PCI-E x16: <%=dat[0].models[MBid].pcie_x16%></p>
    <p>Количество слотов PCI-E x8: <%=dat[0].models[MBid].pcie_x8%></p>
    <p>Количество слотов PCI-E x4: <%=dat[0].models[MBid].pcie_x4%></p>
    <p>Количество слотов PCI-E x1: <%=dat[0].models[MBid].pcie_x1%></p>
    <p>Количество слотов PCI-E x8: <%=dat[0].models[MBid].pcie_x8%></p>
    <p>Основной режим питания: <%=dat[0].models[MBid].pins%> pins</p>
  </main>
</body>
</html>
